server.port=${PORT:8080}
spring.application.name=bloodbank-backend

# Database (Default in-memory H2, fast & persistent during uptime, configurable to Oracle/Postgres if credentials provided)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:h2:mem:bloodbank;MODE=Oracle;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE}
spring.datasource.driver-class-name=${SPRING_DATASOURCE_DRIVER:org.h2.Driver}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:sa}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.open-in-view=true

# Actuator Health Check
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always
